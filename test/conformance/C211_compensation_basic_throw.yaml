name: "C211: Compensation broadcast throw — handler fires, PI finishes"
fixture: "compensation_basic_throw.bpmn"
process_model_id: "CompensationBasicThrow"
tier: auto
start:
  payload: null
expected:
  final_state: "finished"
  final_tokens:
    - endEventId: "End_1"
