name: "C202: Inclusive Gateway split-join — zero truthy, default path taken"
fixture: "inclusive_gateway_default.bpmn"
process_model_id: "InclusiveGatewayDefault"
tier: auto
start:
  payload:
    amount: 1
expected:
  final_state: "finished"
  final_tokens:
    - endEventId: "End_Default"
