defmodule BfwEngineWeb.Admin do
  @moduledoc """
  Public namespace for the admin HTML surface.

  Phase 0 step 16 mounts:

    * An empty `/admin` HTML shell.
    * Swagger UI served from the static `priv/swagger/` directory.
  """
end
