defmodule EvilEngine.Telemetry do
  @moduledoc """
  Public namespace for the in-process `:telemetry` counters.

  Phase 0 only scaffolds the supervision tree. The actual counter set
  + `/stats` snapshot shape land in Phase 1 step 13.
  """
end
