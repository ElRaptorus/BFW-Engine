defmodule BfwEngine.Auth do
  @moduledoc """
  Public namespace for the built-in JWT validator (§13).

  Phase 0 scaffolds the supervision tree. The JWKS cache and the
  `BfwEngine.Auth.JwtVerifier` module land in Phase 0 step 8.
  """
end
