# Local-dev compose stack per `docs/ImplementationPlan.md` §14.2:
# "Two services only: engine + postgres:16. No tracing / metrics sidecars
# in v1 (D24)."
services:
  postgres:
    image: postgres:16-alpine
    container_name: evil-engine-postgres
    # Production pools are 100 write + 50 read; stock Postgres allows 100.
    command: postgres -c max_connections=200
    environment:
      POSTGRES_USER: evil_engine
      POSTGRES_PASSWORD: evil_engine
      POSTGRES_DB: evil_engine_dev
      # Per Schema.md — LZ4 JSONB compression (D39).
      POSTGRES_INITDB_ARGS: "--data-checksums"
    ports:
      - "5432:5432"
    volumes:
      - evil_engine_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "evil_engine", "-d", "evil_engine_dev"]
      interval: 5s
      timeout: 5s
      retries: 12

  engine:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: evil-engine
    environment:
      # §14.3 env contract.
      TDE_DATABASE_HOST: postgres
      TDE_DATABASE_PORT: 5432
      TDE_DATABASE_NAME: evil_engine_dev
      TDE_DATABASE_USER: evil_engine
      TDE_DATABASE_PASS: evil_engine
      TDE_HTTP_PORT: "4000"
      TDE_HTTP_SECRET_KEY_BASE: "BloodForTheBloodGod!_SkullsForTheSkullThrone!"
      TDE_ENGINE_ID: "evil-engine-local"
      TDE_ENGINE_NAME: "Daemon Engine (production)"
      # Payload cap + compression defaults (D39).
      TDE_TOKEN_MAX_BYTES: "65536"
      TDE_JSONB_COMPRESSION: "lz4"
      # Auth — HS256 dev secret. Mint tokens with: mix evil.mint_token
      TDE_JWT_HS256_SECRET: "BloodForTheBloodGod!_SkullsForTheSkullThrone!"
      # Event sinks (D37). DB sink stays off by default.
      TDE_EVENT_SINK_CONSOLE: "on"
      TDE_EVENT_SINK_TELEMETRY: "on"
      TDE_EVENT_SINK_WEBSOCKET: "on"
      # Optional seeding directory (D19). Mount a host folder to use it.
      # TDE_SEEDING_DIRECTORY: "/app/seeding"
    ports:
      - "4000:4000"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:4000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

volumes:
  evil_engine_postgres_data:
